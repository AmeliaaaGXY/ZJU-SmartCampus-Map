# 紫金港校区 WebGIS 项目搭建指南

本文档是一份面向后续开发的 vibe coding 操作指南。它的用途是告诉你每一步应该怎样指挥 Codex、哪些操作需要自己手动完成、哪些功能应该避免做偏。

当前文档只负责规划和提示词，不代表现在已经搭建项目。后续真正开发时，请按步骤逐条复制提示词给 Codex 执行。

---

## 1. 项目定位

本项目定位为：

> 浙江大学紫金港校区 WebGIS 信息点位展示平台

核心目标不是做完整校园导航，而是做一个可演示的 WebGIS 应用：

- 在地图上展示紫金港校区相关点位。
- 支持自习室、校园 POI 等信息展示。
- 支持 AI 自习室推荐查询：用户输入需求，AI 根据已有自习室数据推荐最合适的自习室。
- 支持空数据状态，即使数据组暂时没有交付真实数据，页面也能正常显示。
- 规定统一 GeoJSON 数据格式，方便后续补充真实数据。
- 充电桩查询功能通过接入已有开源项目 ZJU-Charger 完成，不在本项目中重复实现。

推荐技术路线：

```text
前端：Vue 3 + Vite + Leaflet
后端：FastAPI
数据：GeoJSON
空间服务：GeoServer WMS / WFS，用于管理部分展示数据
三维展示：Cesium，用于展示与主题相关的数据
AI 查询：后端代理 DeepSeek 等大模型 API，用于自习室推荐
充电桩：ZJU-Charger 外部接入
Python 环境：D:\env\miniconda\envs\webgis\python.exe
```

---

## 2. 最终功能边界

必须做：

- 紫金港校区地图浏览。
- 自习室点位展示，允许没有真实数据。
- AI 自习室推荐查询，用户输入需求后推荐最合适的自习室。
- 校园 POI 点位展示，允许没有真实数据。
- 统一 GeoJSON 数据格式。
- 空数据兜底页面。
- 充电桩入口接入 ZJU-Charger。
- 通过 GeoServer 管理部分展示数据，例如自习室点位、校园 POI 或建筑轮廓中的一类。
- 与主题相关的数据可在 Cesium 上展示，例如自习室点位、校园 POI 或建筑轮廓。
- 项目说明文档和数据格式文档。

不做：

- 不实现校园导航。
- 不实现路线规划。
- 不实现路径推荐。
- 不重新开发充电桩查询系统。
- 不复制 ZJU-Charger 大量源码。
- 不生成课堂汇报材料。
- 不把 Cesium 做成复杂三维系统，但需要预留或实现至少一类主题数据的三维展示。
- 不做自习室筛选功能，自习室选择逻辑交给 AI 推荐查询。

可选：

- GeoServer 管理更多数据图层。
- Cesium 展示更多主题数据。
- iframe 嵌入 ZJU-Charger。
- UI skill 辅助视觉优化。

---

## 3. 手动环境准备

这些步骤建议你自己在终端中确认，不要一开始就让 Codex 自动安装全部环境。

### 3.1 检查 Python 环境

你的指定 Python 路径是：

```powershell
D:\env\miniconda\envs\webgis\python.exe
```

手动检查：

```powershell
D:\env\miniconda\envs\webgis\python.exe --version
```

如需激活 conda 环境：

```powershell
conda activate webgis
```

注意：Vue 和 Vite 需要 Node.js。Conda 的 Python 环境不能替代 Node.js 环境。

### 3.2 检查 Node 和 Git

手动检查：

```powershell
git --version
node --version
npm --version
```

如果没有 Node.js，建议手动安装 Node.js LTS 版本。

### 3.3 可能需要手动安装的软件

MVP 必需：

```text
Git
Node.js LTS
npm
Miniconda / Conda
浏览器
```

可选：

```text
GeoServer
Java 17 或 Java 21
pnpm
Cesium ion 账号
高德地图 Key
天地图 Key
```

### 3.4 Token 和 Key

MVP 阶段不需要任何 Token。

只有在以下情况才需要手动申请：

- 使用 Cesium ion 地形或影像：需要 Cesium ion Token。
- 使用高德 JS SDK 或高德 Web API：需要高德 Key。
- 使用天地图底图服务：需要天地图 Key。
- 访问私有 GitHub 仓库：可能需要 GitHub Token。

建议先不要申请这些 Key，先用 Leaflet + 公开底图 + 本地 GeoJSON 跑通项目。

---

## 4. Vibe Coding 总原则

每次给 Codex 的提示词建议使用固定结构：

```text
当前状态：
项目已经完成了……

本次目标：
请实现……

具体要求：
1. …
2. …
3. …

限制：
只修改……，不要重构整个项目。

验收标准：
1. …
2. …
3. …
```

不要一次性说：

```text
请帮我完成整个大作业。
```

应该小步推进：

```text
项目规范文档 -> 基础目录 -> 地图页面 -> 数据格式 -> 空数据兜底 -> 点位展示 -> 充电桩接入 -> 后端接口 -> UI 优化 -> 测试验收
```

每一步都要求 Codex：

- 说明修改了哪些文件。
- 不要实现未要求的功能。
- 不要引入大型新依赖。
- 保证项目可运行。
- 出错时优先修复当前问题，不要大范围重写。

---

## 5. 第 0 步：让 Codex 建立项目共识

### 给 Codex 的提示词

```text
请先阅读当前目录下的《大作业方案.md》，并结合以下补充要求，重新定义项目范围。不要写代码。

补充要求：
1. 本项目范围仅限浙江大学紫金港校区；
2. 本项目不需要实现校园导航功能；
3. 不需要路线规划、路径推荐或一键导航；
4. 不需要生成课堂汇报材料；
5. 充电桩查询功能已有开源项目 ZJU-Charger：https://github.com/ZJU-Charger/ZJU-Charger ，我们只需要接入或跳转，不要重新实现完整充电桩系统；
6. 自习室、校园 POI 等点位信息可以先留空，或者只保留少量虚假数据；
7. 必须规定标准数据格式；
8. 即使没有真实数据，页面也要能正常展示空状态，不报错；
9. 自习室不需要筛选功能；
10. 需要 AI 自习室推荐查询功能：用户输入自己的需求，AI 推荐最适合的自习室；
11. AI 可接入 DeepSeek 等大模型 API，API Key 由用户手动配置，不要写入前端代码；
12. 可通过 GeoServer 对部分展示数据进行管理；
13. 与主题相关的数据可在 Cesium 上展示；
14. Python 环境使用：D:\env\miniconda\envs\webgis\python.exe。

请输出新的 MVP 功能清单、技术路线、目录结构建议和开发顺序。
请不要修改文件。
```

### 手动操作

无。

### 验收标准

- Codex 明确项目只覆盖紫金港校区。
- Codex 明确不做校园导航。
- Codex 明确充电桩只是接入 ZJU-Charger。
- Codex 明确空数据也要正常展示。

---

## 6. 第 1 步：生成项目级长期记忆文档

建议创建这些文档：

```text
AGENTS.md
docs/PROJECT_CONTEXT.md
docs/DATA_CONTRACT.md
docs/ZJU_CHARGER_INTEGRATION.md
docs/EMPTY_STATE_POLICY.md
docs/GEOSERVER.md
docs/CESIUM.md
docs/AI_STUDY_ROOM_RECOMMENDER.md
```

其中 `AGENTS.md` 最重要，它相当于后续所有 Codex 会话都要遵守的项目规则。

### 给 Codex 的提示词

```text
请为本项目生成项目级长期记忆文档。

需要创建：
1. AGENTS.md：规定 Codex 后续开发必须遵守的项目范围、技术栈、代码风格、禁止事项；
2. docs/PROJECT_CONTEXT.md：说明项目背景、MVP 范围、已取消功能；
3. docs/DATA_CONTRACT.md：规定自习室、POI、建筑轮廓等 GeoJSON 数据格式；
4. docs/ZJU_CHARGER_INTEGRATION.md：说明 ZJU-Charger 的接入策略；
5. docs/EMPTY_STATE_POLICY.md：说明没有真实数据时页面如何展示；
6. docs/GEOSERVER.md：说明通过 GeoServer 管理部分展示数据的方案；
7. docs/CESIUM.md：说明与主题相关的数据如何在 Cesium 上展示；
8. docs/AI_STUDY_ROOM_RECOMMENDER.md：说明 AI 自习室推荐查询的接口、安全策略、提示词和空数据兜底。

关键约束：
1. 项目范围仅限浙江大学紫金港校区；
2. 不实现校园导航；
3. 不实现路线规划；
4. 不生成课堂汇报材料；
5. 不复制 ZJU-Charger 大量源码；
6. 充电桩功能优先采用后端代理 ZJU-Charger API，iframe 作为备选，外链作为兜底；
7. 所有业务数据允许为空数组；
8. 前端必须有空状态展示，不能因为数据为空报错；
9. 自习室不需要筛选功能；
10. 需要 AI 自习室推荐查询功能；
11. AI API Key 必须放在后端环境变量中，不允许暴露到前端；
12. 可通过 GeoServer 对部分展示数据进行管理；
13. 与主题相关的数据可在 Cesium 上展示；
14. Python 命令优先使用 D:\env\miniconda\envs\webgis\python.exe。

请创建文档，不要搭建功能代码。
```

### 手动操作

无。

### 验收标准

- `AGENTS.md` 写清楚项目边界。
- `docs/DATA_CONTRACT.md` 写清楚 GeoJSON 字段。
- `docs/EMPTY_STATE_POLICY.md` 写清楚空数据策略。
- `docs/GEOSERVER.md` 写清楚至少一类展示数据如何交给 GeoServer 管理。
- `docs/CESIUM.md` 写清楚至少一类主题数据如何在 Cesium 上展示。
- `docs/AI_STUDY_ROOM_RECOMMENDER.md` 写清楚 AI 推荐查询如何接入、如何保护 API Key、空数据时如何兜底。

---

## 7. 第 2 步：规划项目目录，但暂不要求一次性搭完整功能

推荐目录结构：

```text
webgis-campus-platform/
  frontend/
  backend/
  data/
  docs/
  AGENTS.md
  README.md
```

### 给 Codex 的提示词

```text
请基于当前项目创建一个课程大作业项目结构。

技术栈：
1. frontend：Vue 3 + Vite + Leaflet，后续可加入 Cesium 主题数据展示；
2. backend：Python FastAPI，必须使用 D:\env\miniconda\envs\webgis\python.exe 运行；
3. data：存放 GeoJSON mock 数据；
4. docs：存放项目文档；
5. GeoServer：用于管理至少一类展示数据，更多图层可选；
6. AI：通过后端代理 DeepSeek 等大模型 API，提供自习室推荐查询。

要求：
1. 项目范围仅限浙江大学紫金港校区；
2. 不实现校园导航；
3. 不实现路线规划；
4. 不生成课堂汇报材料；
5. 充电桩功能只预留接入 ZJU-Charger；
6. 自习室数据和 POI 数据允许为空；
7. 页面必须能在空数据情况下正常显示；
8. 自习室不需要筛选功能；
9. 需要预留 AI 自习室推荐查询接口；
10. AI API Key 只能放在后端环境变量中；
11. 可通过 GeoServer 对部分展示数据进行管理；
12. 与主题相关的数据可在 Cesium 上展示；
13. 项目结构要简单，适合课程演示。

请完成基础目录、最小 README、前后端依赖说明。完成后告诉我启动命令。
```

### 手动操作

如果 Codex 创建了前端项目，通常需要手动执行：

```powershell
cd frontend
npm install
```

如果 Codex 创建了后端依赖文件，可能需要手动执行：

```powershell
D:\env\miniconda\envs\webgis\python.exe -m pip install fastapi uvicorn python-dotenv
```

### 验收标准

- 目录清晰。
- 没有一次性塞入复杂功能。
- README 有启动说明。

---

## 8. 第 3 步：前端地图页面提示词

### 给 Codex 的提示词

```text
请在 frontend 中实现基础 WebGIS 主页面。

要求：
1. 使用 Vue 3 + Vite；
2. 使用 Leaflet 显示二维地图；
3. 默认定位浙江大学紫金港校区；
4. 页面布局为：左侧功能面板，右侧地图；
5. 左侧面板包含三个模块入口：自习室、校园 POI、充电桩；
6. 暂时不接真实数据；
7. 数据为空时显示“暂无数据，等待数据组补充”，不能报错；
8. 不要实现校园导航；
9. 不要实现路线规划；
10. 不要生成课堂汇报材料。

请只修改 frontend 相关文件。
```

### 手动操作

可能需要安装 Leaflet：

```powershell
cd frontend
npm install leaflet
```

启动前端：

```powershell
npm run dev
```

### 验收标准

- 页面能打开。
- 地图默认定位紫金港校区。
- 左侧面板和地图区域布局稳定。
- 没有真实数据时页面不白屏。

---

## 9. 第 4 步：GeoJSON 数据规范提示词

### 给 Codex 的提示词

```text
请在 data 目录下创建标准 GeoJSON 数据模板。

需要创建：
1. study_rooms.geojson；
2. campus_pois.geojson；
3. buildings.geojson；
4. README.md，说明所有字段含义。

要求：
1. 每个文件必须是合法 GeoJSON FeatureCollection；
2. 可以只放 2-3 条紫金港校区附近的虚假示例数据，也可以为空 features；
3. 坐标系统一 WGS84 / EPSG:4326；
4. geometry 坐标顺序为 [longitude, latitude]；
5. 字段名使用英文小写和下划线；
6. 缺失值用 null、unknown、0 或 false；
7. 前端读取空 features 时不能报错；
8. 不要创建充电桩本地数据，因为充电桩功能接入 ZJU-Charger；
9. 不要实现校园导航。
```

### 推荐自习室字段

```json
{
  "id": "sr_demo_001",
  "name": "示例自习室",
  "building": "示例教学楼",
  "floor": "2F",
  "room": "201",
  "type": "quiet",
  "open_time": "08:00",
  "close_time": "22:30",
  "seat_total": 100,
  "seat_available": 20,
  "has_power": true,
  "description": "演示用虚假数据"
}
```

### 推荐 POI 字段

```json
{
  "id": "poi_demo_001",
  "name": "示例校园点位",
  "category": "library",
  "audience": "both",
  "open_time": "unknown",
  "description": "演示用虚假数据"
}
```

### 手动操作

数据组后续补充真实数据时，需要按 `docs/DATA_CONTRACT.md` 检查字段。

### 验收标准

- GeoJSON 格式合法。
- 坐标位于紫金港校区附近。
- `features: []` 合法。
- 没有充电桩本地数据。

---

## 10. 第 5 步：空数据兜底提示词

### 给 Codex 的提示词

```text
请实现前端 GeoJSON 数据加载逻辑。

要求：
1. 加载 study_rooms.geojson 和 campus_pois.geojson；
2. 如果文件不存在、features 为空、字段缺失，都不能导致页面崩溃；
3. 地图上没有点位时，左侧显示清晰空状态；
4. 如果有示例数据，则显示 marker 和弹窗；
5. 弹窗字段缺失时显示“未知”；
6. 数据读取逻辑要封装，避免散落在组件里；
7. 不要实现校园导航；
8. 不要接入充电桩本地数据。
```

### 手动操作

手动把 `features` 改成空数组测试：

```json
{
  "type": "FeatureCollection",
  "features": []
}
```

### 验收标准

- 空数据页面不白屏。
- 控制台没有明显报错。
- 字段缺失时显示“未知”。
- 地图仍然可缩放、拖拽。

---

## 11. 第 6 步：自习室与 POI 展示提示词

### 给 Codex 的提示词

```text
请基于现有 Leaflet 页面，实现自习室和校园 POI 的地图展示。

要求：
1. 自习室和 POI 使用不同颜色或图标；
2. 左侧面板可以切换“自习室”和“校园 POI”；
3. 点击列表项时地图定位到对应点位；
4. 点击地图 marker 时显示弹窗；
5. 自习室不需要筛选功能，只需要列表展示、地图定位和详情弹窗；
6. POI 可以保留简单 category 展示，但不强制做筛选；
7. 数据为空时展示空状态；
8. 所有点位必须默认位于浙江大学紫金港校区范围附近；
9. 不要实现路线规划或校园导航；
10. 不要生成课堂汇报材料。
```

### 手动操作

无。

### 验收标准

- 自习室和 POI 图层可区分。
- 列表和地图 marker 能联动。
- 自习室没有筛选 UI。
- 空数据不报错。

---

## 12. 第 7 步：AI 自习室推荐查询提示词

AI 查询功能用于替代传统筛选。用户输入自己的需求，例如“我想找一个安静、有插座、晚上十点后还能学习、离图书馆近的地方”，后端把需求和当前自习室数据发给 DeepSeek 等大模型 API，由 AI 返回推荐结果。

推荐架构：

```text
前端输入框 -> FastAPI /api/ai/recommend-study-room -> 后端读取 study_rooms.geojson -> 后端调用 DeepSeek 等 AI API -> 返回推荐自习室和理由
```

不要在前端直接调用大模型 API，因为 API Key 会泄露。API Key 必须放在后端 `.env`。

### 给 Codex 的提示词

```text
请为项目设计并实现 AI 自习室推荐查询功能。

功能目标：
用户在前端输入自己的学习需求，AI 根据当前 study_rooms.geojson 中的自习室数据，推荐最适合的一个或多个自习室，并给出推荐理由。

要求：
1. 自习室不需要筛选功能；
2. 前端提供一个“AI 推荐自习室”输入框和提交按钮；
3. 后端新增 POST /api/ai/recommend-study-room；
4. 后端读取 study_rooms.geojson，把用户需求和候选自习室摘要发送给 DeepSeek 等 AI API；
5. AI API Key 只能从后端环境变量读取，例如 DEEPSEEK_API_KEY；
6. 不允许把 API Key 写入前端代码、Git 仓库或文档示例真实值；
7. 如果没有 API Key，后端返回清晰提示，并可使用本地规则兜底推荐；
8. 如果 study_rooms.geojson 的 features 为空，返回“暂无自习室数据，无法推荐”，前端不能报错；
9. AI 返回内容需要包含：推荐自习室名称、推荐理由、匹配到的用户需求、注意事项；
10. 如果 AI 返回的自习室名称无法匹配现有数据，前端只展示文字推荐，不强制地图定位；
11. 如果能匹配到现有点位，则地图定位到对应 marker；
12. 不要实现校园导航；
13. 不要实现路线规划；
14. 不要生成课堂汇报材料。
```

### 手动操作

如果决定接入 DeepSeek，你需要手动：

```text
1. 注册 DeepSeek 或其他大模型 API 平台账号；
2. 创建 API Key；
3. 把 API Key 写入后端 .env；
4. 不要把 .env 提交到 Git；
5. 在 README 或 docs/AI_STUDY_ROOM_RECOMMENDER.md 中只写变量名，不写真实 Key。
```

`.env` 示例：

```text
DEEPSEEK_API_KEY=你的_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
AI_RECOMMENDER_ENABLED=true
```

可能需要安装依赖：

```powershell
D:\env\miniconda\envs\webgis\python.exe -m pip install httpx
```

### 本地规则兜底建议

没有 API Key 时，可以让后端用简单规则推荐：

```text
1. 优先 seat_available > 0；
2. 用户提到“安静”，优先 type=quiet；
3. 用户提到“讨论/小组”，优先 type=discussion；
4. 用户提到“通宵/晚上”，优先 type=overnight；
5. 用户提到“插座/充电”，优先 has_power=true；
6. 最后按 seat_available 从高到低排序。
```

### 验收标准

- 前端有 AI 查询输入框。
- 自习室没有筛选 UI。
- 后端有 `/api/ai/recommend-study-room`。
- API Key 不出现在前端代码中。
- 没有 API Key 时有清晰提示或本地规则兜底。
- 空数据时不报错。
- 推荐结果能展示理由；匹配到点位时地图能定位。

---

## 13. 第 8 步：ZJU-Charger 接入提示词

ZJU-Charger 仓库：

```text
https://github.com/ZJU-Charger/ZJU-Charger
```

默认外链地址：

```text
https://charger.philfan.cn/
```

推荐接入优先级：

```text
方案 A：后端代理调用 ZJU-Charger API
方案 B：iframe 嵌入公开页面
方案 C：外链跳转到 https://charger.philfan.cn/
不推荐：复制或改造 ZJU-Charger 大量源码
```

### 给 Codex 的提示词

```text
请阅读 ZJU-Charger 仓库公开信息：https://github.com/ZJU-Charger/ZJU-Charger ，并为本项目制定充电桩功能接入方案。

要求：
1. 优先考虑通过本项目 FastAPI 后端代理 ZJU-Charger API；
2. 先说明需要确认哪些接口、CORS、授权、限流和稳定性问题；
3. 备选考虑 iframe 嵌入；
4. 外链跳转到 https://charger.philfan.cn/ 作为最终兜底；
5. 不要复制 ZJU-Charger 大量源码；
6. 说明 GPLv3 许可证对课程项目的注意事项；
7. 输出到 docs/ZJU_CHARGER_INTEGRATION.md；
8. 不要实现校园导航；
9. 不要生成课堂汇报材料。
```

### 手动操作

你需要手动确认：

- 打开 `https://charger.philfan.cn/`，确认网站可访问。
- 确认 ZJU-Charger 公开 API 的 base URL、endpoint、参数和响应字段。
- 确认 API 是否需要授权、是否有限流、是否允许课程项目访问。
- 如果 iframe 被网站安全策略拦截，则改为外链按钮。
- 如果 API 不稳定或未配置，则使用 iframe 或外链兜底。

### 验收标准

- 文档说明清楚采用 API 优先、iframe 备选、外链兜底。
- 不复制 ZJU-Charger 源码。
- 前端有清晰的充电桩 API 状态和兜底入口。

---

## 14. 第 9 步：FastAPI 后端提示词

如果课程要求必须有后端，建议做最小 FastAPI 后端。

### 给 Codex 的提示词

```text
请在 backend 中实现最小 FastAPI 后端。

运行 Python 必须使用：
D:\env\miniconda\envs\webgis\python.exe

接口要求：
1. GET /api/health：返回项目状态；
2. GET /api/study-rooms：读取 data/study_rooms.geojson；
3. GET /api/pois：读取 data/campus_pois.geojson；
4. GET /api/config：返回 ZJU-Charger 外链地址和功能开关；
5. POST /api/ai/recommend-study-room：根据用户需求和自习室数据返回 AI 推荐；
6. 所有接口都要允许数据为空；
7. 文件不存在时返回空 FeatureCollection，而不是 500；
8. 添加 CORS，方便前端开发环境访问；
9. AI API Key 从后端环境变量读取，不要写入前端。

限制：
1. 不实现校园导航；
2. 不实现路线规划；
3. 不实现本地充电桩数据库；
4. 不实现自习室筛选功能；
5. 不生成课堂汇报材料；
6. 不复制 ZJU-Charger 源码。
```

### 手动操作

可能需要安装依赖：

```powershell
D:\env\miniconda\envs\webgis\python.exe -m pip install fastapi uvicorn python-dotenv httpx
```

启动命令通常类似：

```powershell
D:\env\miniconda\envs\webgis\python.exe -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

具体以 Codex 生成的后端入口文件路径为准。

### 验收标准

- `/api/health` 正常。
- 数据文件不存在时不返回 500。
- 空数据返回合法 FeatureCollection。
- `/api/ai/recommend-study-room` 在无 API Key 或空数据时有安全兜底。
- CORS 正常。

---

## 15. 第 10 步：前端请求后端提示词

### 给 Codex 的提示词

```text
请把前端数据来源从直接读取本地 GeoJSON 改为请求后端 API。

接口：
1. GET http://127.0.0.1:8000/api/study-rooms；
2. GET http://127.0.0.1:8000/api/pois；
3. GET http://127.0.0.1:8000/api/config；
4. POST http://127.0.0.1:8000/api/ai/recommend-study-room。

要求：
1. 使用 axios；
2. API 地址放到环境变量 VITE_API_BASE_URL；
3. 后端不可用时，前端回退到空数据状态；
4. 页面显示“后端暂不可用，当前展示空数据/示例数据”；
5. AI 推荐接口失败时显示清晰错误提示，不要白屏；
6. 自习室不需要筛选 UI；
7. 不要因为请求失败导致白屏；
8. 不要实现校园导航；
9. 不要实现路线规划；
10. 不要生成课堂汇报材料。
```

### 手动操作

可能需要安装 axios：

```powershell
cd frontend
npm install axios
```

可能需要创建或检查 `.env`：

```text
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_CHARGER_URL=https://charger.philfan.cn/
VITE_CHARGER_EMBED_MODE=false
```

### 验收标准

- 后端开启时能加载 API 数据。
- 后端关闭时页面不白屏。
- 充电桩入口配置来自后端或环境变量。
- AI 推荐接口失败时页面有提示。

---

## 16. 第 11 步：GeoServer 管理部分展示数据提示词

GeoServer 不再只是文档预留，而是建议至少管理一部分展示数据。最稳的选择是让 GeoServer 管理一类简单图层，例如 `study_rooms`、`campus_pois` 或 `buildings` 中的一类。

如果时间紧，优先选择 `campus_pois.geojson` 或 `study_rooms.geojson` 发布为 WMS/WFS；前端仍保留本地 GeoJSON / 后端 API 兜底，避免 GeoServer 未启动时页面白屏。

### 给 Codex 的提示词

```text
请为项目增加 GeoServer 对接能力，用 GeoServer 管理部分展示数据。

要求：
1. 在 docs/GEOSERVER.md 写明如何把 study_rooms.geojson、campus_pois.geojson 或 buildings.geojson 中至少一类数据发布成 WMS/WFS；
2. 推荐优先发布 campus_pois 或 study_rooms，数据简单、便于演示；
3. 前端配置中增加 VITE_GEOSERVER_URL、VITE_GEOSERVER_WORKSPACE、VITE_GEOSERVER_LAYER；
4. 地图代码中实现或预留 addWmsLayer 函数，用于加载 GeoServer WMS 图层；
5. 如需点选查询，说明 WFS 或 GetFeatureInfo 的接入方式；
6. 如果没有 GeoServer 地址或 GeoServer 未启动，默认回退到本地 GeoJSON / 后端 API 展示；
7. 不要因为 GeoServer 不可用导致页面白屏；
8. 不要实现校园导航；
9. 不要实现路线规划；
10. 不要生成课堂汇报材料。
```

### 手动操作

如果真的要部署 GeoServer，可能需要手动完成：

- 安装 Java。
- 安装 GeoServer。
- 启动 GeoServer。
- 创建 workspace。
- 导入 GeoJSON 或 Shapefile。
- 发布 WMS / WFS。
- 把服务地址和图层名写入 `.env`。

`.env` 示例：

```text
VITE_GEOSERVER_URL=http://127.0.0.1:8080/geoserver
VITE_GEOSERVER_WORKSPACE=webgis
VITE_GEOSERVER_LAYER=campus_pois
VITE_USE_GEOSERVER=false
```

如果时间紧，至少让 Codex 写清楚 GeoServer 发布步骤和前端配置项；如果能部署，则至少发布一类展示数据。

### 验收标准

- 至少有一类展示数据被规划为可由 GeoServer 管理。
- 文档能说明 WMS/WFS 发布步骤。
- 前端没有硬编码失效服务地址。
- 没有 GeoServer 时项目仍可运行。

---

## 17. 第 12 步：Cesium 主题数据展示提示词

Cesium 用于满足“与主题相关的数据可在 Cesium 上展示”的要求。不要把它做成复杂三维导航系统，建议只做一个轻量三维视图：展示同一批自习室或校园 POI 点位，或者展示建筑轮廓。

### 给 Codex 的提示词

```text
请为项目增加 Cesium 主题数据展示能力。

要求：
1. 不影响现有 Leaflet 二维地图；
2. 增加一个“三维展示”入口或视图切换；
3. Cesium 默认定位浙江大学紫金港校区；
4. 至少展示一类与主题相关的数据，例如 study_rooms、campus_pois 或 buildings；
5. 优先复用现有 GeoJSON 数据；
6. 如果 Cesium ion Token 未配置，使用不需要 Token 的基础方案或显示清晰提示；
7. Cesium 数据为空时显示空状态，不报错；
8. 点击 Cesium 点位时显示名称、类型、描述等基础信息；
9. 不要实现三维路线规划；
10. 不要实现校园导航；
11. 不要生成课堂汇报材料。
```

### 手动操作

可能需要安装 Cesium：

```powershell
cd frontend
npm install cesium
```

如果使用 Cesium ion 地形、影像或资产，需要手动申请 Cesium ion Token，并写入 `.env`：

```text
VITE_CESIUM_ION_TOKEN=你的_token
```

如果不使用 Cesium ion 服务，可以先不申请 Token，只做基础三维视图和点位展示。

### 验收标准

- Leaflet 二维地图仍然可用。
- Cesium 视图能打开。
- 至少一类主题数据能在 Cesium 上展示。
- 数据为空或 Token 缺失时不白屏。
- 没有路线规划或导航功能。

---

## 18. 第 13 步：UI 优化提示词

UI 优化要在核心功能稳定后做。

### 给 Codex 的提示词

```text
请优化当前项目 UI，让它更像一个课程级 WebGIS 应用，而不是默认 demo。

要求：
1. 不改变业务逻辑；
2. 地图区域保持最大；
3. 左侧面板信息清晰；
4. 空状态要好看；
5. AI 自习室推荐查询入口要明显，但不要遮挡地图；
6. 自习室、POI、充电桩入口视觉区分明确；
7. 不要设计自习室筛选器；
8. 按钮、标签、弹窗风格统一；
9. 移动端不崩；
10. 不要做营销首页；
11. 不要实现校园导航；
12. 不要实现路线规划；
13. 不要生成课堂汇报材料。

修改前请先说明 UI 优化计划，修改后说明改了哪些文件。
```

### 是否使用 UI skill

可以用，但不是必须。

推荐使用时机：

- 功能已经稳定。
- 页面能正常启动。
- 地图、点位、空状态都能显示。
- 只需要优化视觉和交互细节。

不建议：

- 一开始就用 UI skill。
- 让 UI skill 重构地图逻辑。
- 让 UI skill 改后端接口。

如果使用 ui-ux-pro-max-skill，可以给 Codex：

```text
请使用 ui-ux-pro-max-skill 为本项目生成设计系统建议。

项目类型：课程 WebGIS 应用
主题：浙江大学紫金港校区信息点位展示平台
技术栈：Vue 3 + Leaflet + FastAPI
页面：地图主界面、左侧面板、AI 自习室推荐查询、点位弹窗、空状态、充电桩外链入口

限制：
1. 不要营销首页；
2. 不要重构业务逻辑；
3. 不要实现校园导航；
4. 不要实现路线规划；
5. 不要生成课堂汇报材料。
```

### 手动操作

如果使用外部 skill，需要你手动安装对应 skill，并确认 Codex 当前会话能识别它。

### 验收标准

- UI 更像完整工具。
- 地图区域没有被装饰元素挤压。
- 空数据状态清晰。
- 移动端不崩。

---

## 19. 第 14 步：测试与验收提示词

### 给 Codex 的提示词

```text
请完整检查当前项目。

重点检查：
1. frontend 是否能 npm run dev；
2. backend 是否能用 D:\env\miniconda\envs\webgis\python.exe 启动；
3. /api/health 是否正常；
4. /api/study-rooms 和 /api/pois 在空数据时是否正常；
5. /api/ai/recommend-study-room 在有数据、空数据、无 API Key 时是否都有合理响应；
6. 前端在后端关闭时是否不会白屏；
7. Leaflet 地图是否正常显示；
8. 自习室和 POI 数据为空时是否显示空状态；
9. 自习室页面是否没有筛选 UI；
10. AI 自习室推荐查询是否能显示推荐结果或清晰错误提示；
11. 充电桩入口是否能打开 ZJU-Charger；
12. GeoServer 配置缺失或服务未启动时，页面是否仍可运行；
13. 是否至少规划或接入了一类 GeoServer 管理的展示数据；
14. Cesium 视图是否能展示至少一类主题相关数据；
15. Cesium Token 缺失或数据为空时是否不会白屏；
16. 控制台是否有明显报错；
17. 是否误实现了校园导航功能；
18. 是否误生成了课堂汇报材料。

发现问题请直接修复，并列出修复内容。
```

### 手动测试清单

```text
1. 关闭后端，只开前端，页面不白屏；
2. 开启后端，接口正常；
3. 把 GeoJSON features 改成 []，页面正常；
4. 删除某个字段，弹窗显示“未知”；
5. 不配置 DEEPSEEK_API_KEY，AI 查询显示清晰提示或使用本地规则兜底；
6. 配置 DEEPSEEK_API_KEY 后，AI 查询能返回推荐结果；
7. 自习室界面没有筛选器；
8. 点击 ZJU-Charger 外链，能打开；
9. 移动端窄屏下侧边栏和地图不重叠；
10. GeoServer 未启动时页面不白屏；
11. Cesium 视图能打开，并展示至少一类主题数据或清晰空状态；
12. 页面中没有校园导航、路线规划、路径推荐功能。
```

### 验收标准

- 主要页面可打开。
- 空数据可展示。
- 示例数据可展示。
- AI 自习室推荐查询可用或有清晰兜底。
- 外部充电桩入口可用。
- GeoServer 对部分展示数据有管理方案或可用图层。
- Cesium 可展示与主题相关的数据。
- 没有明显报错。

---

## 20. 数据组交付格式

数据组至少交付：

```text
study_rooms.geojson
campus_pois.geojson
```

可选交付：

```text
buildings.geojson
```

如果需要 GeoServer 或 Cesium 展示效果更完整，建议优先补充：

```text
campus_pois.geojson：适合 GeoServer WMS/WFS 和 Cesium 点位展示
study_rooms.geojson：适合 Leaflet / GeoServer / Cesium 统一展示
buildings.geojson：适合 Cesium 三维或二维建筑轮廓展示
```

统一要求：

```text
坐标系：WGS84 / EPSG:4326
坐标顺序：[longitude, latitude]
范围：浙江大学紫金港校区
字段名：英文小写加下划线
缺失值：null、unknown、0 或 false
文件格式：合法 GeoJSON FeatureCollection
允许空数据：features: []
```

自习室字段建议：

```text
id
name
building
floor
room
type
open_time
close_time
seat_total
seat_available
has_power
noise_level
power_outlet_level
group_study
overnight_available
nearby_facilities
description
```

自习室 `type` 枚举：

```text
quiet
discussion
overnight
unknown
```

为了 AI 推荐更准确，建议数据组尽量补充这些字段：

```text
noise_level：quiet / normal / lively / unknown
power_outlet_level：none / limited / many / unknown
group_study：true / false
overnight_available：true / false
nearby_facilities：附近设施，例如图书馆、食堂、教学楼
tags：字符串数组，例如 ["安静", "插座多", "适合小组讨论"]
```

POI 字段建议：

```text
id
name
category
audience
open_time
description
```

POI `category` 枚举建议：

```text
library
teaching
canteen
scenic
service
museum
other
```

POI `audience` 枚举建议：

```text
student
visitor
both
```

给数据组的话可以直接复制：

```text
请交付 study_rooms.geojson 和 campus_pois.geojson。所有数据仅限浙江大学紫金港校区，坐标系为 WGS84 / EPSG:4326，坐标顺序为 [longitude, latitude]。字段名使用英文小写加下划线。没有数据时也请交付合法空 FeatureCollection，即 {"type":"FeatureCollection","features":[]}。不要交付截图或只有中文地点名的表格。
```

---

## 21. 代码组分工建议

你负责：

```text
1. Codex 提示词编排；
2. 前端地图页面；
3. UI 和演示效果；
4. AI 自习室推荐查询前端交互；
5. 空数据展示；
6. README 和用户说明；
7. 和数据组确认 GeoJSON 字段。
```

另一位代码同学负责：

```text
1. FastAPI 后端；
2. 数据读取接口；
3. AI 推荐接口和 DeepSeek API 接入；
4. ZJU-Charger 接入测试；
5. GeoServer 数据发布与图层配置；
6. Cesium 主题数据展示；
7. 最终运行环境检查。
```

两个人协作约定：

```text
1. 每次只让 Codex 改一个小功能；
2. 改完立刻运行；
3. 能跑再提交；
4. 不在同一个文件里同时让两个 Codex 会话大改；
5. 每次把报错原样贴给 Codex；
6. 不允许 Codex 擅自加入导航功能。
7. 不允许 Codex 擅自加入自习室筛选功能。
```

---

## 22. 最终验收标准

项目完成时应满足：

```text
1. 项目范围仅限浙江大学紫金港校区；
2. 不实现校园导航；
3. 不实现路线规划；
4. 不生成课堂汇报材料；
5. 地图页面可正常打开；
6. 自习室和 POI 有标准 GeoJSON 数据格式；
7. 数据为空时页面不报错；
8. 有少量 demo 数据时地图可显示 marker；
9. 自习室没有筛选功能；
10. 有 AI 自习室推荐查询功能；
11. AI API Key 不暴露在前端或 Git 仓库中；
12. 充电桩功能能跳转或接入 ZJU-Charger；
13. GeoServer 可管理至少一类展示数据，或有清晰的发布步骤和前端配置；
14. 与主题相关的数据可在 Cesium 上展示，至少覆盖自习室、POI 或建筑轮廓中的一类；
15. 后端使用 D:\env\miniconda\envs\webgis\python.exe 可启动；
16. 前端和后端启动说明清楚；
17. 有项目级 AGENTS.md；
18. 有数据格式文档；
19. 有 ZJU-Charger 接入说明；
20. 有 AI 自习室推荐查询说明；
21. 有空数据策略说明。
```

默认决策：

```text
前端：Vue 3 + Vite + Leaflet
后端：FastAPI
Python：D:\env\miniconda\envs\webgis\python.exe
地图数据：GeoJSON
真实数据：允许为空
自习室筛选：不做
AI 推荐：通过后端代理 DeepSeek 等 API，API Key 放后端环境变量
充电桩：API 优先，iframe 备选，外链兜底
导航：不做
路线规划：不做
Cesium：用于展示至少一类主题相关数据，复杂三维功能可选
GeoServer：用于管理至少一类展示数据，更多图层可选
UI skill：功能稳定后再使用
课堂汇报材料：不生成
```
