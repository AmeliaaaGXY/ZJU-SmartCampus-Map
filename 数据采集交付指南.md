# 紫金港校区 WebGIS 数据采集交付指南

这份文档给数据组同学使用。请按这里的要求采集和整理自习室、校园 POI、建筑轮廓等数据。

## 先看结论

本项目二维地图底图使用 `WGS84` 坐标体系。

同学们需要采集的是：

- 自习室数据；
- 校园 POI 数据；
- 可选的建筑轮廓数据。

同学们不需要采集充电桩数据。充电桩来自 ZJU-Charger，它的坐标由代码单独处理。

如果用手机指南针、手机系统定位、GPS 工具采集经纬度，通常得到的是 `WGS84` 坐标，可以直接用于本项目的自习室和 POI 数据。

不要把百度地图坐标、高德地图坐标、手机指南针坐标混在同一个最终 GeoJSON 文件里。

## 1. 经纬度怎么采集

### 推荐采集方式

推荐使用以下方式之一：

- 手机指南针 App 中显示的经纬度；
- 手机系统定位或 GPS 工具；
- 能明确导出 `WGS84` 坐标的地图工具；
- 实地记录点位名称，再回到电脑上用支持 WGS84 的地图工具校正。

采集时请记录：

```text
longitude：经度
latitude：纬度
coordinate_system：WGS84
```

GeoJSON 中坐标顺序必须是：

```text
[longitude, latitude]
```

也就是先写经度，再写纬度。

### 手机指南针可以直接用吗

可以。  

如果手机指南针显示的是经纬度，通常可以按 `WGS84` 记录，直接用于自习室和 POI 点位。

但请注意：

- 经度一般是 `120.xxxxxx`；
- 纬度一般是 `30.xxxxxx`；
- 不要写反成 `[latitude, longitude]`；
- 不要把手机指南针坐标再拿去百度地图或高德地图里转换；
- 如果你不确定坐标系，请在原始表格的 `notes` 里写“坐标系不确定”。

### 百度地图或高德地图坐标能不能用

不建议作为自习室和 POI 的最终坐标。

如果确实用了百度地图或高德地图，请一定在原始表格里标明：

```text
coordinate_system：BD-09 或 GCJ-02
```

代码组需要先转换后才能放进最终 GeoJSON。

## 2. 最终交付文件

请交付以下文件：

```text
study_rooms.geojson
campus_pois.geojson
buildings.geojson（可选）
原始采集表.xlsx 或 原始采集表.csv
现场照片文件夹（可选）
```

说明：

- `study_rooms.geojson`：自习室点位；
- `campus_pois.geojson`：校园 POI 点位；
- `buildings.geojson`：建筑轮廓，可选；
- 不要创建充电桩 GeoJSON；
- 不要采集导航路线或路径规划数据。

如果某类数据暂时没有，也请交空 GeoJSON：

```json
{
  "type": "FeatureCollection",
  "features": []
}
```

## 3. 通用 GeoJSON 要求

所有 GeoJSON 文件必须满足：

```text
文件格式：GeoJSON FeatureCollection
坐标系：WGS84
坐标顺序：[longitude, latitude]
字段命名：英文小写 + 下划线
缺失值：null、unknown、0、false 或 []
范围：浙江大学紫金港校区附近
```

正确示例：

```json
"coordinates": [120.0869, 30.3046]
```

错误示例：

```json
"coordinates": [30.3046, 120.0869]
```

## 4. 自习室数据

文件名：`study_rooms.geojson`

每个自习室点位建议采集以下字段：

| 字段名 | 含义 | 示例 |
|---|---|---|
| id | 唯一编号 | sr_001 |
| name | 自习室名称 | 紫金港图书馆二楼自习区 |
| building | 所在建筑 | 紫金港图书馆 |
| floor | 楼层 | 2F |
| room | 房间号或区域名 | East Area |
| type | 类型 | quiet / discussion / overnight / unknown |
| open_time | 开放时间 | 08:00 |
| close_time | 关闭时间 | 22:30 |
| seat_total | 总座位数 | 120 |
| seat_available | 当前可用座位数，不清楚填 0 | 0 |
| has_power | 是否有插座 | true / false |
| noise_level | 安静程度 | quiet / normal / lively / unknown |
| power_outlet_level | 插座数量 | none / limited / many / unknown |
| group_study | 是否适合小组讨论 | true / false |
| overnight_available | 是否支持通宵 | true / false |
| nearby_facilities | 附近设施 | ["食堂", "图书馆"] |
| tags | 标签 | ["安静", "插座多"] |
| description | 简短说明 | 适合个人复习 |

这些字段会用于 AI 自习室推荐，所以越真实越好。

## 5. 校园 POI 数据

文件名：`campus_pois.geojson`

每个 POI 点位建议采集以下字段：

| 字段名 | 含义 | 示例 |
|---|---|---|
| id | 唯一编号 | poi_001 |
| name | POI 名称 | 紫金港图书馆 |
| category | 类型 | library / teaching / canteen / scenic / service / museum / other |
| audience | 适用人群 | student / visitor / both |
| open_time | 开放时间 | 08:00-22:30 或 unknown |
| description | 简短说明 | 校园主要图书馆 |

POI 不要求一次性覆盖全校，可以先采集几个重点点位。

## 6. 建筑轮廓数据

文件名：`buildings.geojson`

建筑轮廓是可选数据。不会绘制 Polygon 的话，可以暂时不交。

如果要交，建议使用 QGIS、geojson.io 或其他地图工具绘制。

字段建议：

| 字段名 | 含义 | 示例 |
|---|---|---|
| id | 唯一编号 | bld_001 |
| name | 建筑名称 | 紫金港图书馆 |
| type | 建筑类型 | library / teaching / dorm / service / other |
| height | 建筑高度，不知道填 null | null |
| floors | 楼层数，不知道填 null | 5 |
| description | 简短说明 | 图书馆建筑轮廓 |

## 7. 原始采集表

建议先用 Excel 或 CSV 采集，再整理成 GeoJSON。

表头建议：

```text
id,name,data_type,category,building,floor,room,longitude,latitude,coordinate_system,open_time,close_time,description,collector,updated_at,notes
```

字段说明：

- `data_type`：study_room / poi / building；
- `longitude`：经度；
- `latitude`：纬度；
- `coordinate_system`：建议填 WGS84；
- `collector`：采集人；
- `updated_at`：采集日期，例如 2026-06-08；
- `notes`：不确定的信息写在这里。

## 8. GeoJSON 示例

自习室示例：

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": {
        "type": "Point",
        "coordinates": [120.0869, 30.3046]
      },
      "properties": {
        "id": "sr_001",
        "name": "示例自习室",
        "building": "示例建筑",
        "floor": "2F",
        "room": "201",
        "type": "quiet",
        "open_time": "08:00",
        "close_time": "22:30",
        "seat_total": 100,
        "seat_available": 0,
        "has_power": true,
        "noise_level": "quiet",
        "power_outlet_level": "many",
        "group_study": false,
        "overnight_available": false,
        "nearby_facilities": ["食堂"],
        "tags": ["安静", "插座多"],
        "description": "示例数据，请替换为真实采集内容"
      }
    }
  ]
}
```

POI 示例：

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": {
        "type": "Point",
        "coordinates": [120.087, 30.304]
      },
      "properties": {
        "id": "poi_001",
        "name": "示例 POI",
        "category": "library",
        "audience": "both",
        "open_time": "unknown",
        "description": "示例数据，请替换为真实采集内容"
      }
    }
  ]
}
```

## 9. 交付前检查清单

交付前请确认：

- 文件名是否正确；
- 是否是合法 GeoJSON；
- `features` 是否是数组；
- 坐标顺序是否为 `[longitude, latitude]`；
- 坐标系是否是 `WGS84`；
- 点位是否都在紫金港校区附近；
- 缺失值是否用了 `null`、`unknown`、`0`、`false` 或 `[]`；
- 没有提交充电桩本地数据；
- 没有提交导航路线或路径规划数据。

## 10. 最终打包结构

建议按以下结构打包：

```text
webgis_data_delivery/
  study_rooms.geojson
  campus_pois.geojson
  buildings.geojson
  原始采集表.xlsx
  photos/
```

如果某类数据暂时没有，就交空 GeoJSON，不要省略文件。
